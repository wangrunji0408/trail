# TRAIL — 游戏设计文档

一个伪装成贪吃蛇的网格解谜游戏。蛇从固定起点生长，通过精心设计的关卡，隐喻大语言模型的核心原理——但表面上，这只是一个纯粹的解谜游戏。

## 核心机制

- **网格**：每一关是一个 2D 网格
- **蛇**：从固定起点生长。玩家每步选择方向（WASD/方向键），蛇头朝该方向延伸一格。蛇身不动。
- **不可穿越**：蛇不能穿过自己的身体
- **走回撤销**：方向键指向蛇身倒数第二节时，自动回退一步
- **彩色格**：蛇经过时该节染上对应颜色（红/蓝/绿/黄）
- **序列**：蛇身中所有有色段按顺序排列，忽略无色段
- **图案门**：门上标有颜色序列。蛇的序列中包含该子序列时门才会开（忽略无色段）
- **出口**：蛇头到达出口格即通关

## 关键抽象

| 游戏概念 | LLM 对应概念 |
|---------|-------------|
| 蛇（轨迹） | Token 序列 / 上下文 |
| 蛇的每一节 | 一个 Token |
| 格子颜色 | Token 的语义类型 |
| 起点固定、只能生长 | 自回归生成 |
| 蛇不能穿过自己 | Token 不可修改 |

## 世界与机制

### World 1：生长（Completion）

彩色格 + 图案门。蛇经过彩色格染色，门检查序列是否包含指定子序列。顺序决定成败。

### World 2：回声（Conversation）

检查点 `</>`。蛇进入检查点后，自动沿当前方向延伸若干步，颜色为玩家最后颜色的取反。检查点本身不带色，自动延伸段标记为"回应"（虚线边框）。

### World 3：棱镜（System Prompt）

染料格 `▼` + 待定格 `◇`。蛇经过染料格时，地图上所有 ◇ 立即变为该染料的颜色。引入绿色。玩家通过选择不同的染料路径来改变整个关卡。

### World 4：暗影（Thinking / CoT）

暗区（紫色底纹）。蛇在暗区中的段标记为"暗影"——暗影段占据空间、能触发开关，但对图案门不可见（不计入序列匹配）。

### World 5：分叉（Fork）

Tab 键随时分叉 / 切换分支。分叉后两条蛇独立移动。当一条分支的头走到另一条分支的身体上时，自动合并：两条分支的段拼接为一条蛇，切换到目标分支的头继续。两条分支的开关等副作用都保留。

### World 6：倒带（Rewind）

Backspace 回收蛇头。蛇缩短一节，释放空间。但永久开关不会因倒带复位。

### World 7：遗忘（Context Window / Compaction）

蛇身有长度上限。超过时最老的段自动消失。记忆石 `★`：当一个有色段在记忆石位置消失时，记忆石吸收该颜色，永久保留，计入序列匹配。

### World 8：传送（Tool Call）

传送门 `◎`。蛇进入后画面切换到子网格。在子网格中收集的颜色带回主网格，注入到传送门位置的段上。

### World 9：委派（Subagent）

委派格 `⊕`。蛇进入后选择指令（1/2/3），子代理在子网格中按指令自动执行，返回收集到的颜色。不同指令→不同路径→不同结果。

## 关卡格式

每关为独立 txt 文件，存放在 `levels/` 目录。

```
// world: 1
// title: 生长
// note: 设计师注释（不显示给玩家）
// gate: rb
// solution: sddddwd

. . b r . #
S . # # G E
. . r b . #
```

子关卡用 `---` 分隔：

```
S r @ . G E

--- sublevel
// title: 子空间
S y . E
```

字符约定：

```
.  空地    #  墙      S  起点    E  出口
r  红      b  蓝      g  绿      y  黄
G  图案门  C  检查点   @  传送门  $  委派格
!a 永久开关  |a 开关墙  ~  暗区    ~b 暗区+蓝
Dr 红染料   Dg 绿染料  ◇  待定格   *r 记忆石+红
```

解法字符：`w↑ a← s↓ d→ < 倒带 t 分叉/切换 1/2/3 选择 {} 子空间`
