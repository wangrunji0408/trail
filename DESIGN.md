# TRAIL — 游戏设计文档

## 一句话概述

一个伪装成贪吃蛇的网格解谜游戏。玩家在网格上延伸一条不断生长的"蛇"（轨迹），通过精心设计的关卡逐步学会大语言模型的全部核心原理——但表面上，这只是一个纯粹的解谜游戏。

---

## 核心机制

### 基本规则

- **网格**：每一关是一个 2D 网格（约 8×8 到 16×16）
- **蛇（轨迹）**：从固定的起点开始生长。玩家每步选择一个方向（上下左右），蛇头朝该方向延伸一格。蛇身保持不动。
- **不可穿越**：蛇不能穿过自己的身体（和贪吃蛇一样）
- **格子（Tile）**：网格上的格子有不同类型，蛇经过时会产生效果
- **目标**：让蛇头到达出口（Exit）格，即为通关

### 关键抽象

| 游戏概念 | LLM 对应概念 |
|---------|-------------|
| 蛇（轨迹） | Token 序列 / 上下文 |
| 蛇的每一节 | 一个 Token |
| 格子颜色 | Token 的语义类型 |
| 起点固定、只能生长 | 自回归生成（只能 append，不能改历史） |
| 蛇不能穿过自己 | Token 一旦生成不可修改 |

> **设计原则**：游戏中绝不出现任何 LLM 术语。所有概念通过纯游戏机制传达。玩家在玩到第 3-4 世界时会恍然大悟："这不就是 LLM 吗？"

---

## 世界与关卡设计

### World 1：生长（对应：Completion / Token 生成）

**新机制：彩色格子 + 图案门**

- **彩色格子**：蛇经过时，该节身体会染上对应颜色（红🔴 / 蓝🔵 / 绿🟢）
- **图案门（Pattern Gate）**：一种特殊的墙。门上标有一个颜色序列（如 🔴🔵🔴）。只有当蛇身的**最近 N 节**恰好匹配该序列时，门才会打开
- **空格**：蛇经过后该节为白色（无色）

**设计意图**：玩家必须规划路径，以正确的顺序经过彩色格子，使蛇身末端形成正确的"序列"来打开门。这就是 **Completion**——生成正确的 token 序列。

**示例关卡**：

```
关卡 1-1：入门
┌─────────┐
│ S · · · │    S = 起点
│ · ■ ■ · │    E = 出口
│ · · · · │    ■ = 墙
│ · · · E │
└─────────┘
纯路径规划，无特殊机制。学会基本操作。

关卡 1-3：你的颜色很重要
┌───────────┐
│ S · 🔴 · · │
│ · ■ ■ · · │
│ · · · 🔵 · │
│ · [🔴🔵] E │    [🔴🔵] = 图案门，要求末尾两节为红蓝
└───────────┘
玩家必须先经过红格再经过蓝格，使蛇身末端为...🔴🔵，门才会开。
如果先蓝后红，门上的图案不匹配，无法通过。

关卡 1-5：顺序陷阱
门要求 🔴🔵🔴，但地图上红蓝格的位置迫使你绕路。
最短路径得到的是 🔵🔴🔵——错误！必须找更长但顺序正确的路径。
```

**玩家学到的**：序列的顺序至关重要，不仅是"包含"什么，而是末尾的精确排列。

---

### World 2：回声（对应：Conversation / Input-Output）

**新机制：预置蛇身 + 回合制**

- **预置段（Input Segments）**：关卡开始时，蛇已经有一些身体节段（用不同的视觉样式显示，如虚线边框）。玩家从预置段的末尾开始延伸。
- **检查点（Checkpoint）**：蛇头到达检查点后，会基于固定规则生成新的预置段附加到蛇上，然后玩家继续延伸。这模拟了"对话轮次"。
- **回声门（Echo Gate）**：类似图案门，但检查的是**预置段**的颜色，而非玩家延伸的部分。

**生成规则示例**：
- 检查点 A：如果玩家最后一节是 🔴，新增预置段 🔵；如果是 🔵，新增 🔴
- 检查点 B：复制玩家最后两节颜色作为新预置段

**设计意图**：玩家意识到自己的"输出"会影响下一轮的"输入"。这就是**对话**——prompt 影响 response，response 又成为新的 prompt 的一部分。

**示例关卡**：

```
关卡 2-1：回声
┌─────────────┐
│ ═══S · · · │    ═══ = 预置段（蓝蓝蓝）
│ · · ■ · · │
│ · 🔴 · 🔵 · │
│ · · [回声门] E │    回声门检查预置段，要求预置段含🔴
└─────────────┘
预置段全是蓝色，回声门检查预置段是否有红——不满足！
但到达检查点后，检查点会根据玩家路径生成新预置段。
玩家需要先经过 🔴，到达检查点，让系统把 🔴 加入预置段，
然后回声门就能打开了。

关卡 2-4：对话链
有 3 个检查点，每个检查点的生成规则不同。
玩家必须在每一轮精心选择颜色，使得链式生成的结果最终打开终点的门。
这是一个多轮对话——你必须从第一句话就开始规划。
```

**玩家学到的**：输入决定输出，输出又变成下一轮输入。整个蛇身就是完整的"对话历史"。

---

### World 3：棱镜（对应：System Prompt）

**新机制：棱镜 + 变色效应**

- **棱镜（Prism）**：关卡开始前，玩家从几个颜色中选择一个"棱镜色"
- **棱镜格（Prism Tile）**：标有 ◇ 的格子。其实际行为取决于当前棱镜色：
  - 棱镜色=🔴：所有 ◇ 格变成 🔴 格
  - 棱镜色=🔵：所有 ◇ 格变成 🔵 格
  - 棱镜色=🟢：所有 ◇ 格变成墙
- **棱镜门（Prism Gate）**：门的要求图案也会被棱镜色影响
- 棱镜色在关卡中途**不可更改**

**设计意图**：棱镜是一个**全局设定**，在游戏开始前确定，影响整个关卡的一切行为。这就是 **System Prompt**——预先设定的"人格/角色"改变模型的所有后续行为。

**示例关卡**：

```
关卡 3-2：一色定乾坤
┌───────────┐
│ S · ◇ · · │    有 3 个棱镜格和 2 个门
│ · ■ · ◇ · │
│ · [🔴🔵] · · │    门要求红蓝序列
│ · · ◇ · E │
└───────────┘
选棱镜色=🔴：所有 ◇ 变红格。但门要求红蓝，地图上没有蓝格来源——死局。
选棱镜色=🔵：所有 ◇ 变蓝格。能凑出蓝，但没有红——死局。
选棱镜色=🟢：所有 ◇ 变墙。路径完全改变，但露出了隐藏的红蓝格——可解！
```

**玩家学到的**：开始前的一个选择，彻底改变了整个世界的规则。和 World 2 的预置段结合后，棱镜 + 预置段 + 延伸 = System + User + Assistant。

---

### World 4：暗影（对应：Thinking / Chain of Thought）

**新机制：暗区 + 暗影蛇身**

- **暗区（Shadow Zone）**：地图上用暗色底纹标记的区域
- 蛇在暗区内延伸的身体节段变为**暗影段（Shadow Segment）**——半透明显示
- 暗影段**占据空间**（蛇不能穿过），**能触发开关和机关**
- 但暗影段**不算入图案门的检查**——门"看不见"它们
- **实体化（Solidify）**：特殊格子，蛇经过后所有暗影段变为实体段（可被门看见）
- **消散（Dismiss）**：特殊格子，蛇经过后所有暗影段消失（释放空间，但长度缩短）

**设计意图**：暗影段是"思考过程"——它影响世界状态（触发开关），但不出现在"输出"（图案门检查）中。玩家学会用暗影段来"思考"——按开关、探路——然后消散，让实际"输出"保持干净。这就是 **Thinking**——模型内部推理，不显示给用户，但影响最终输出。

**示例关卡**：

```
关卡 4-3：暗中摸索
┌─────────────────┐
│ S · · [🔴] ·  E │    门要求一节红色
│ · ████ · ■  · · │    ████ = 暗区
│ · █🔵█ · ·  · · │    暗区中有蓝格和开关
│ · █⚡█ · 🔴 · · │    ⚡ = 开关（控制墙的开关）
│ · ████ · ■→· · │    ■→· = 开关控制的墙
└─────────────────┘
问题：🔴 格被墙挡住了。开关 ⚡ 在暗区里。
解法：
1. 蛇进入暗区，触发开关 ⚡（墙打开）—— 暗影段可以触发开关
2. 蛇回到明区，路过 🔴 格（现在可达了）
3. 到达门 [🔴]——门只检查非暗影段，看到末尾是 🔴，通过！
如果不走暗区而想绕路到开关，路径太长，无法保持正确的颜色序列。
```

**玩家学到的**：有些"思考步骤"对结果至关重要，但不应该出现在最终序列里。

---

### World 5：分叉（对应：Fork / Beam Search）

**新机制：分叉格 + 合并格**

- **分叉格（Fork Tile）**：蛇头到达此格后，蛇**分裂**为两条（或多条）蛇
  - 每条蛇独立控制（玩家在蛇之间切换，或并行控制）
  - 每条蛇继承分叉点之前的完整蛇身
- **合并格（Merge Tile）**：任意一条蛇到达此格时，玩家**必须选择保留哪条蛇**
  - 被放弃的蛇消失，释放空间
  - 但被放弃的蛇**已触发的开关、已改变的状态保留**
- 未合并前，所有蛇共享同一张地图，互相阻挡

**设计意图**：分叉让你同时探索多个可能性，然后选择最好的一条路。被放弃的分支不是白费——它留下的副作用是关键。这就是 **Fork**——并行采样多个 completion，选择最好的。

**示例关卡**：

```
关卡 5-2：二选一？都选
┌─────────────────┐
│ S · · · [F] · · │    F = 分叉格
│ · · · ↗ · ↖ · · │    蛇分成两条
│ · · ⚡₁ · · ⚡₂ · │    两个开关分别控制不同的门
│ · · · · [M] · · │    M = 合并格
│ · · 🚪₁ · 🚪₂ · E │    需要两扇门都开才能到 E
└─────────────────┘
分叉后，蛇 A 去按 ⚡₁，蛇 B 去按 ⚡₂。
两个开关都被按下后，合并时无论选哪条蛇，两扇门都已经开了。
关键洞察：你选择保留的那条蛇的颜色序列决定了能否过最终的图案门。
```

**玩家学到的**：探索多条路径，利用副作用，选择最优解。

---

### World 6：倒带（对应：Rewind / Regeneration）

**新机制：倒带按钮**

- **倒带（Rewind）**：新的操作——玩家可以"回收"蛇尾（从蛇头端缩短 1 节）
  - 缩短的格子变回可通行状态
  - 该格子上的颜色效果取消
  - 但**永久开关（Permanent Switch）** 不会因倒带而复位
- **快照格（Snapshot Tile）**：蛇经过后设下一个"存档点"。倒带不能倒退超过最近的快照点。

**设计意图**：倒带就是**撤销最近生成的 token**，重新生成。但有些副作用是不可逆的（永久开关）。这就是 **Rewind**——回退并重新生成，但对话中已经产生的影响无法完全消除。

**示例关卡**：

```
关卡 6-3：建了又拆
┌──────────────┐
│ S · · 🔴 · · │
│ · · · · ⚡ₚ · │    ⚡ₚ = 永久开关
│ · ■→· · · · │    ■→· = 被永久开关控制的墙
│ · · · 🔵 · · │
│ · · [🔵🔴] · E │    门要求蓝红序列
└──────────────┘
1. 蛇先往右走，经过 🔴，到达 ⚡ₚ 按下永久开关（墙打开）
2. 倒带！回收刚才的路径，蛇缩回到起点附近
3. 现在墙已经打开了（永久开关不复位），走新路径
4. 先经过 🔵 再经过 🔴，形成蓝红序列，通过门
```

**玩家学到的**：可以"尝试"一段路径来获取信息或触发效果，然后撤销重来。但历史的某些痕迹无法擦除。

---

### World 7：遗忘（对应：Context Window / Compaction）

**新机制：长度上限 + 浓缩**

- **长度上限**：蛇身有最大长度（显示为一个计量条）。超过上限时，蛇**从尾巴（起点端）开始褪色**——最老的节段自动消失
- 褪色的节段释放空间，但其颜色从蛇身中移除（影响图案门判定）
- **记忆石（Memory Stone）**：特殊格子。如果一个节段在褪色时正好位于记忆石上，该记忆石会**吸收**这个颜色，永久显示
  - 图案门会把记忆石的颜色视为蛇身的一部分（视为蛇身最早的部分）
- **浓缩格（Compact Tile）**：蛇经过时，将蛇身最老的 3 节**压缩为 1 节**，颜色取三节中出现最多的颜色。这节省了长度但丢失了精确信息

**设计意图**：蛇不能无限长——这就是**上下文窗口**。当旧的"token"不得不丢弃时，记忆石就是**摘要/compaction**——用精简的方式保留关键信息。

**示例关卡**：

```
关卡 7-4：记忆管理
长度上限 = 8。地图较大（12×12）。
从起点到终点的最短路径 = 15 步（超过上限）。
路上有一个门要求 [🔴🔵🔴] 的序列。

问题：走到门时，早期经过的 🔴 已经褪色消失了！
解法：在路径早期经过 🔴 时，确保蛇身上的 🔴 位于记忆石上。
当 🔴 节段褪色时，记忆石吸收了红色，门依然认为序列完整。
或者：使用浓缩格在合适时机压缩蛇身，保留关键颜色。
```

**玩家学到的**：记忆有限，必须策略性地决定保留什么、压缩什么、舍弃什么。

---

### World 8：传送（对应：Tool Call）

**新机制：传送门 + 子网格**

- **传送门（Portal）**：蛇头到达传送门时，画面切换到一个独立的**子网格（Sub-Grid）**
  - 子网格是一个独立的小型关卡
  - 子网格有自己的起点和出口
  - 在子网格中，蛇用**独立的蛇身**（不影响主网格蛇身长度）
- **返回物（Return Item）**：子网格的出口处有一个彩色格子。蛇到达出口时，该颜色作为"结果"带回主网格
- 回到主网格后，返回物的颜色**插入蛇身**（作为传送门位置的颜色）
- **参数门（Argument Gate）**：某些传送门上有图案要求——蛇身必须匹配特定图案才能进入（"函数签名"）

**设计意图**：传送门是**工具调用**。你离开当前上下文，进入一个独立的执行环境，获得结果后带回来。子网格中发生的事不影响主网格的蛇身（tool call 的内部过程对主上下文透明），只有最终结果被注入。

**示例关卡**：

```
关卡 8-2：取回你需要的
┌──────────────┐
│ S · · · 🟣 · │    🟣 = 传送门
│ · · · · · · │
│ · [🔴🟡] · · E │    门要求红黄序列
│ · · · 🔴 · · │    地图上有红格但没有黄格！
└──────────────┘
主地图上没有黄色格子。但传送门通向一个子网格：
┌─────────┐
│ s · 🟡 · │    子网格中有黄格
│ · · · e │    到达 e 时带回黄色
└─────────┘
解法：先经过 🔴，再进入传送门获得 🟡，蛇身变成 ...🔴🟡，门开。

关卡 8-5：链式调用
传送门 A 的子网格中包含传送门 B（嵌套调用！）。
必须从最内层开始解决，逐层带回结果。
```

**玩家学到的**：有些问题不能在当前"上下文"中解决，需要调用外部工具。工具的内部细节被隐藏，只有结果可见。

---

### World 9：模仿（对应：SFT / Supervised Fine-Tuning）

**新机制：示范回放 + 规则变异**

- **示范阶段**：关卡开始前，播放一段"幽灵蛇"的自动演示
  - 幽灵蛇在一个"示范网格"上自动行走，玩家观看
  - 示范网格和玩家的实际网格**结构相似但不完全相同**
- **学习效应**：示范结束后，玩家的实际网格会发生变化：
  - 幽灵蛇经常经过的格子类型（如颜色格子的颜色）会"渗透"到玩家网格的对应位置
  - 幽灵蛇的路径偏好会影响某些格子的行为（如门的要求模式可能变化）
  - 效果：玩家的网格被"微调"过了
- **多次示范**：有些关卡播放多个示范，效果叠加

**设计意图**：观看"专家演示"后，游戏世界本身发生了变化——这就是 **SFT**。模型通过观看专家数据，调整了自身的行为模式（权重）。玩家不需要精确模仿示范，但示范改变了规则，使新的解法成为可能。

**示例关卡**：

```
关卡 9-2：学以致用
示范网格：幽灵蛇走了一条 🔴🔴🔵🔴 的路径通关。
学习效应：玩家网格中，原本要求 [🔵🔵🔴🔵] 的门变成了要求 [🔴🔴🔵🔴]。
           原本的空白格在幽灵蛇经过的对应位置变成了 🔴 格。
玩家利用这些变化，用新的策略通关。

关卡 9-5：坏示范
有意展示一个"差"的示范——学习效应使关卡变得更难！
玩家必须找到绕过"坏学习"带来的障碍的方法。
（映射：SFT 数据质量对模型的影响）
```

**玩家学到的**：从示范中"学习"改变了规则本身。好的示范使问题更容易，坏的示范使问题更难。

---

### World 10：演化（对应：RL / Reinforcement Learning）

**新机制：多轮尝试 + 动态地形**

- **评分**：每个格子有一个分值（+1, -1, 0 等，显示为小数字）。蛇通过时累计得分。
- **通关条件变更**：不仅要到达出口，还要达到**目标分数**
- **多轮机制**：同一关可以玩多次。每次结束后：
  - 蛇走过的高分格子分值**下降**（"探索惩罚"）
  - 蛇没走过的格子分值**上升**（"好奇心奖励"）
  - 某些格子会根据你的路径**改变类型**（如空地变墙、墙变空地）
- **收敛**：经过 3-5 轮后，分值和地形趋于稳定，"最优路径"浮现
- **最终轮**：提示"最终尝试"，此轮的分数才是最终成绩

**设计意图**：玩家亲身体验了 **RL** 的过程——反复尝试，环境基于你的行为给出反馈（奖励塑形），逐渐收敛到最优策略。地形的变化映射了策略梯度更新。

**示例关卡**：

```
关卡 10-1：适者生存
第 1 轮：所有格子分值均匀（都是 +1）。玩家走了一条直线到出口。得 6 分。
第 2 轮：直线路径上的格子变成 0 分，旁边的格子变成 +2。玩家被引向新路径。得 8 分。
第 3 轮：地形进一步变化，一条之前不存在的路径出现了。走这条路得 12 分。
第 4 轮（最终）：地形稳定。最优路径清晰可见。目标分数 = 12。

关卡 10-4：奖励黑客
一个精心设计的关卡，玩家可以找到一种"钻漏洞"的走法——
获得超高分但明显不是设计者想要的路径。
（映射：Reward hacking，RL 中模型找到奖励函数漏洞的现象）
```

**玩家学到的**：通过反复试错和环境反馈，最优策略会自然浮现。但奖励设计不当会导致"作弊"行为。

---

## 组合关卡（World 11+：融合）

后期关卡混合多个世界的机制，创造更深层的谜题：

### 组合示例

| 关卡 | 机制组合 | LLM 映射 |
|------|---------|----------|
| 11-1 | 棱镜 + 预置段 + 暗影 | System Prompt + 对话 + Thinking：用棱镜设定"人格"，在对话轮次中用暗影"思考"再输出 |
| 11-3 | 传送门 + 暗影 | Tool call + Thinking：在暗影中"决定"调用哪个工具，进入传送门执行 |
| 11-5 | 分叉 + 倒带 + 遗忘 | Fork + Rewind + Compaction：分叉探索后倒带，但上下文窗口限制了你能保留多少信息 |
| 11-7 | 模仿 + 演化 | SFT + RL：先用示范"微调"规则，再用多轮演化进一步优化 |
| 12-1 | 全部机制 | 完整的 LLM pipeline：选棱镜（system prompt）→ 阅读预置段（user input）→ 暗影区思考 → 分叉探索 → 进传送门调用工具 → 管理遗忘 → 输出答案 |

---

## 视觉与美学

### 风格
- **极简主义**：纯色块 + 几何图形，无像素画无卡通
- **色彩**：深色背景，蛇身和格子用鲜明的色彩
- **蛇的外观**：连续的彩色色带，像一条彩虹蛇在网格中蜿蜒
- **无文字教学**：每个新机制通过 1-2 个不可能失败的教学关卡引入

### 音效
- 蛇延伸：清脆的"嗒"声
- 门打开：和弦音
- 倒带：倒放音效
- 分叉：分裂音效
- 传送：空间穿越音效

---

## 游戏名称

**TRAIL**

含义层次：
1. 表面：蛇留下的"轨迹/痕迹"
2. 隐含：Token 序列就是一条 Trail
3. 谐音：Trial（尝试）——每一关都是一次尝试

---

## 恍然大悟时刻

游戏不需要在任何地方明说"这是 LLM"。但以下设计会让了解 LLM 的玩家逐渐意识到：

1. **World 1-2**：蛇在生长，序列很重要，有输入输出轮次→ "这有点像……"
2. **World 3**：选择一个全局设定改变一切 → "这不就是 system prompt 吗？"
3. **World 4**：暗影思考不影响输出 → "Thinking！这就是 Chain of Thought！"
4. **World 7**：上下文窗口限制！→ "天哪这整个游戏就是 LLM！"
5. **World 8**：工具调用 → 确认无疑
6. **World 9-10**：SFT 和 RL → 完全明了

对于不了解 LLM 的玩家，这就是一个纯粹的、精妙的解谜游戏。通关后如果有人告诉他们"你刚才玩的就是 LLM 的原理"，他们会：
- 对 LLM 有直觉层面的理解
- 理解 token、上下文窗口、thinking 等概念不再是抽象的

---

## 技术规格

- **平台**：Web（HTML5 Canvas 或 WebGL）
- **框架**：可用纯 TypeScript + Canvas，或 Phaser.js
- **关卡格式**：JSON 定义网格、格子类型、规则
- **无需 LLM 运行时**：所有规则硬编码，游戏逻辑完全确定性
- **关卡编辑器**：后期可加入，允许玩家设计和分享关卡

---

## 实现优先级（Demo 版本）

### Phase 1：核心引擎 + World 1
- 网格渲染
- 蛇的生长与碰撞
- 彩色格子 + 图案门
- 5 个教学关卡

### Phase 2：World 2-4
- 预置段与检查点
- 棱镜系统
- 暗影系统
- 每个世界 5 个关卡

### Phase 3：World 5-8
- 分叉与合并
- 倒带系统
- 长度上限与浓缩
- 传送门与子网格

### Phase 4：World 9-10 + 组合关卡
- 示范回放系统
- 多轮评分系统
- 融合关卡
