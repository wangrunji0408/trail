# TRAIL — 游戏设计文档

一个伪装成贪吃蛇的网格解谜游戏。蛇从固定起点生长，通过精心设计的关卡，隐喻大语言模型的核心原理——但表面上，这只是一个纯粹的解谜游戏。

## 核心机制

- **网格**：每一关是一个 2D 网格
- **蛇**：从固定起点生长。玩家每步选择方向（WASD/方向键），蛇头朝该方向延伸一格。蛇身不动。
- **不可穿越**：蛇不能穿过自己的身体
- **走回撤销**：方向键指向蛇身倒数第二节时，自动回退一步
- **符号格**：蛇经过时该节获得对应符号。符号分两类：
  - **字符**：`0-9`, `+-*/=` — 代表 token 内容
  - **角色标记**：`U`(user/蓝), `A`(assistant/绿), `S`(system/红), `T`(think/灰)
- **序列**：蛇身中所有有符号的段按顺序排列，忽略无符号段
- **图案门**：门上标有符号序列。蛇的序列中包含该连续子串时门才会开
- **T（think）的特殊性**：T 后、下一个 U/A/S 前的内容在序列匹配时被忽略（隐喻思考过程对用户不可见）
- **出口**：蛇头到达出口格即通关

## 关键抽象

| 游戏概念 | LLM 对应概念 |
|---------|-------------|
| 蛇（轨迹） | Token 序列 / 上下文 |
| 蛇的每一节 | 一个 Token |
| 符号格 | Token 的语义内容 |
| 角色标记 U/A/S/T | 消息角色：user/assistant/system/thinking |
| 起点固定、只能生长 | 自回归生成 |
| 蛇不能穿过自己 | Token 不可修改 |
| T 后内容对门不可见 | 思考过程对用户不可见 |

## 世界与机制

### World 1：Token（Completion）

符号格 + 图案门。蛇经过符号格获得字符，门检查序列是否包含指定子串。顺序决定成败。暗示 prefill。

### World 2：Decode

✨ 格。蛇进入 ✨ 时，对当前序列应用 decode 规则（正则匹配），匹配则获得对应符号，否则获得 `?`。暗示 decode/采样。

### World 3：对话（Conversation）

引入 `U`(user/蓝) 和 `A`(assistant/绿) 角色标记。蛇经过角色格后段带上对应颜色。

### World 4：系统提示（System Prompt）

引入 `S`(system/红) 角色标记。

### World 5：思考（Thinking / CoT）

引入 `T`(think/灰) 角色标记。T 后的内容在门匹配时被忽略，直到遇到下一个 U/A/S。

### World 6：分叉（Fork）

Tab 键随时分叉 / 切换分支。分叉后两条蛇独立移动。当一条分支的头走到另一条分支的身体上时，自动合并：两条分支的段拼接为一条蛇，切换到目标分支的头继续。两条分支的开关等副作用都保留。

### World 7：遗忘（Context Window / Compaction）

蛇身有长度上限。超过时最老的段自动消失。记忆石 `★`：当一个有符号段在记忆石位置消失时，记忆石吸收该符号，永久保留，计入序列匹配。

### World 8：传送（Tool Call）

传送门 `◎`。蛇进入后画面切换到子网格。在子网格中收集的符号带回主网格，注入到传送门位置的段上。

### World 9：委派（Subagent）

委派格 `⊕`。蛇进入后选择指令（1/2/3），子代理在子网格中按指令自动执行，返回收集到的符号。不同指令→不同路径→不同结果。

## 关卡格式

每关为独立 txt 文件，存放在 `levels/` 目录。

```
// world: 1
// title: Token
// gate: 1+1=2
// solution: sddddwd

^ . 1 + 1 #
. . # # G E
. . . = 2 #
```

子关卡用 `---` 分隔：

```
^ S @ . G E

--- sublevel
// title: 子空间
^ U . E
```

Decode 规则（一行一条，pattern 为正则表达式）：

```
// decode: 1\+1=$ 2
// decode: 2\+2=$ 4
```

字符约定：

```
.  空地    #  墙      ^  起点    E  出口
U  user    A  asst    S  system  T  think
0-9 数字   +-*/= 运算符
G  图案门  ✨ decode   @  传送门  $  委派格
!a 永久开关  |a 开关墙  F  分叉
◇  待定格   *S 记忆石+符号
```

解法字符：`w↑ a← s↓ d→ < 倒带 t 分叉/切换 1/2/3 选择 {} 子空间`
